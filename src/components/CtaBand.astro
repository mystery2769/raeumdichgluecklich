---
const { title, text, buttonLabel, href = '/kontakt/' } = Astro.props;
---
<section class="band">
  <div class="wrap band-in">
    <div class="band-copy">
      <h3>{title}</h3>
      <p>{text}</p>
    </div>
    <a href={href} class="btn">{buttonLabel}</a>
  </div>
</section>
